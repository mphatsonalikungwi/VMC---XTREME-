(()=>{'use strict';
/* Step 2: My Membership — additive presentation upgrade only. Existing data/actions remain authoritative. */
const boot=()=>{if(document.getElementById('vmcMembershipUpgrade'))return;const card=document.querySelector('.layout > section.card:not(.sub)');const status=document.getElementById('status'),start=document.getElementById('start'),expiry=document.getElementById('expiry');if(!card||!status||!start||!expiry)return;
const style=document.createElement('style');style.id='vmcMembershipUpgrade';style.textContent=`
.vmc-membership-summary{margin:15px 0 12px;padding:14px 15px;border:1px solid rgba(255,255,255,.07);border-radius:16px;background:rgba(255,255,255,.025);display:grid;gap:10px}.vmc-membership-progress{height:6px;border-radius:99px;background:#24272e;overflow:hidden}.vmc-membership-progress span{display:block;height:100%;width:0;border-radius:99px;background:#e31b2d;transition:width .35s ease}.vmc-membership-note{margin:0;color:#8f96a0;font-size:11px}.vmc-membership-note strong{color:#cfd3d9}
@media(max-width:760px){.vmc-membership-summary{padding:13px}}
`;document.head.appendChild(style);
const summary=document.createElement('div');summary.className='vmc-membership-summary';summary.innerHTML='<div class="vmc-membership-progress" aria-hidden="true"><span id="vmcMembershipProgress"></span></div><p class="vmc-membership-note" id="vmcMembershipNote">Your membership dates are shown below.</p>';
const details=card.querySelector('.details');if(!details)return;card.insertBefore(summary,details);
const parseDate=v=>{if(!v)return null;const d=new Date(String(v).includes('T')?v:`${v}T00:00:00`);return Number.isNaN(d.getTime())?null:d};
const refresh=()=>{const s=String(status.textContent||'').trim(),a=String(start.textContent||'').trim(),e=String(expiry.textContent||'').trim();const sd=parseDate(a),ed=parseDate(e),today=new Date();let pct=0,note='Your membership dates are shown below.';if(sd&&ed&&ed>=sd){const total=Math.max(1,ed-sd),elapsed=Math.min(total,Math.max(0,today-sd));pct=Math.max(0,Math.min(100,elapsed/total*100));const remaining=Math.max(0,Math.ceil((ed-today)/86400000));note=remaining===0?'Membership expires today.':`${remaining} day${remaining===1?'':'s'} remaining in this membership.`}else if(s==='Payment Being Reviewed'){note='Your payment is being reviewed by VMC. Your membership dates will update after verification.'}else if(!sd||!ed){note='Membership dates will appear after VMC confirms your membership.'}document.getElementById('vmcMembershipProgress').style.width=`${pct}%`;document.getElementById('vmcMembershipNote').innerHTML=note.includes('remaining')?`<strong>${note}</strong>`:note};
refresh();const observer=new MutationObserver(refresh);[status,start,expiry].filter(Boolean).forEach(x=>observer.observe(x,{childList:true,characterData:true,subtree:true}));setTimeout(()=>observer.disconnect(),15000)};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();